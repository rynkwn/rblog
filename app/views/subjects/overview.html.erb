<% provide(:title, @subject.name + " blogs") %>
<% provide(:page_desc, "SUBJECT: " + @subject.name) %>

<h1 class="text-left"><%= @subject.name %> Blogs</h1>

<% @blogs.reverse.each do |blog| %>
  <%= render partial: "universal/blog_summary", :locals => { :blog => blog }%>
<% end %>